frontend:
  type: frontend
  url_path: /
  repo_path: .
  dev:
    command: ["silly-demo"]
    dockerfile: Dockerfile.dev
  prod:
    dockerfile: Dockerfile
  resources:
  - name: silly-demo
    engine: postgres
    version: 13
    type: database
